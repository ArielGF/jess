name: Deploy to Webempresa

on:
  push:
    branches:
      - master

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Check out the code
        uses: actions/checkout@v2

      - name: Upload files using FTP
        env:
          FTP_HOST: "178.33.115.23"
          FTP_USER: "desarrollo@jessica2-cp27.wordpresstemporal.com"
          FTP_PASS: "arielDesarrollo2024"
        run: |
          find . -type f -exec curl -T {} ftp://$FTP_USER:$FTP_PASS@$FTP_HOST/public_html/desarrollo/{} \;
